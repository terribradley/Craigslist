import { payGrade } from 'craigslist/helpers/pay-grade';
import { module, test } from 'qunit';

module('Unit | Helper | pay grade');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = payGrade([42]);
  assert.ok(result);
});
